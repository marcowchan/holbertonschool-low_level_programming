#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - print all digits in base 10
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%i", i);
	putchar('\n');
	return (0);
}

